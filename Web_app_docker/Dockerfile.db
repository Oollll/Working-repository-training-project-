# Dockerfile for db service
FROM python:3.9

# Install SQLite (though it is typically included with Python)
RUN apt-get update && apt-get install -y sqlite3

# Create directory for SQLite data
RUN mkdir -p /var/lib/sqlite

# Add a default command to keep the container running
CMD ["tail", "-f", "/dev/null"]
